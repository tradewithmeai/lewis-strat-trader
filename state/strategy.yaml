# Active strategy configuration.
# Edit this file to change strategy parameters. The system reads it on each
# reflection cycle. Do NOT change the strategy name here — use state/comparison.json
# to monitor challengers and switch manually when a GREEN challenger appears.

strategy: breakout
params:
  entry_period: 40
  exit_period: 20
  stop_loss_pct: 8.0
