# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uv/
dist/
build/

# State (runtime files — not tracked)
state/comparison.json
state/alerts.jsonl
state/status.json
state/trades.jsonl
state/paper_trades.jsonl
state/heartbeat.json
state/hypotheses.jsonl
state/history/
state/commit_log.tsv

# Throwaway validation scripts generated during research
_val_*.py

# Parquet lake — lives in crypto-lake-rs, not here
*.parquet
*.parquet.bad

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Signal capture runtime data
state/signals/
state/lake_rollup/

# Live paper-trade runtime ledger (per-machine, written by paper_trader)
state/paper_accounts.json
state/status.json

# CCE (code-context-engine)
# CCE local cache (per-machine, not for version control)
.cce/
# Claude Code local settings written by cce init
.claude/settings.local.json
# CCE init may leave scored-backup snapshots beside ignored state files
state/*.score-bak
